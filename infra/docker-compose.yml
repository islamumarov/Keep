version: '1.0'
services:
  gateway:
    build: '../src/gateway/'
    ports:
      - '8080:8080'
    environment:
      - ASPNETCORE_URLS=http://+:8080
    depends_on:
      - auth
      - todo
  
   
